# API Documentation

Coming soon — API documentation and examples for Smart Plug AI.

This file will include:

- REST API endpoints (authentication, device registration, telemetry retrieval)
- MQTT topics and payload examples for telemetry and commands
- Example curl and client usage for common tasks (register device, publish telemetry)
- Auth flow (JWT or Firebase/Supabase) and required headers
- Rate limits and pagination conventions

Initial placeholder endpoints to document:
- POST /api/v1/devices/register
- POST /api/v1/devices/:id/telemetry
- GET /api/v1/devices/:id/history
- POST /api/v1/auth/login

Include example payloads and expected responses when available.
